{{!-- <h1>{{message}}</h1>
<h2>{{status}}</h2>
<pre>{{error.stack}}</pre> --}}
{{!-- <html> --}}

{{!--

<head>
  <style>
    @import url('https://fonts.googleapis.com/css?family=Roboto+Mono'); //

    helper .center-xy {
      // width: inherit; top: 50%; left: 50%; transform:
      translate(-50%, -50%);
      position: absolute;
    }

    html,
    body {
      font-family:
        'Roboto Mono', monospace;
      font-size: 16px;
    }

    html {
      box-sizing:
        border-box;
      user-select: none;
    }

    body {
      background-color: ;
    }

    *,
    *:before,
    *:after {
      box-sizing: inherit;
    }

    .container {
      width: 100%;
    }

    .copy-container {
      text-align: center;
    }

    p {
      color: #000;
      font-size: 24px;
      letter-spacing: .2px;
      margin: 0;
    }

    .handle {
      background: ;
      width: 14px;
      height: 30px;
      top: 0;
      left: 0;
      margin-top: 1px;
      position: absolute;
    }

    #cb-replay {
      fill: #666;
      width: 20px;
      margin: 15px;
      right: 0;
      bottom: 0;
      position: absolute;
      overflow: inherit;
      cursor: pointer;

      &:hover {
        fill:
          #888;
      }
    }

    body {
      width: 100%;
      height: 100%;
      font-family: 'Raleway',
        sans-serif;
      font-weight: 300;
      margin: 0;
      padding: 0;
    }

    #title {
      text-align: center;
      font-size: 40px;
      margin-top: 40px;
      margin-bottom: -40px;
      position: relative;
      color: #000;
    }
  </style>
</head> --}}
<style>
  p {
    color: #000;
    font-size: 24px;
    letter-spacing: .2px;
    margin: 0;
  }

  #title {
    text-align: center;
    font-size: 40px;
    margin-top: 40px;
    margin-bottom: -40px;
    position: relative;
    color: #000;
  }

  .container {
    width: 100%;
  }

  .copy-container {
    text-align: center;
  }
</style>
<div class="container ">
  <div id="title">Something Wrong
    <br /><br />
    <div class="copy-container center-xy align-items-center">

      <p>
        {{code}}
        {{message}}
      </p>

      <span class="handle"></span>

    </div>
  </div>
</div>